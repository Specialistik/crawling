#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../yarn-cli/yarn-debug.js" "$@"
  ret=$?
else 
  node  "$basedir/../yarn-cli/yarn-debug.js" "$@"
  ret=$?
fi
exit $ret
